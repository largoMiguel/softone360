# Incrementar el tamaño máximo del body de las peticiones
# Necesario para permitir subida de imágenes en Base64
# Cálculo: 4 imágenes × 2MB × 1.33 (Base64) = ~10.64MB + overhead del JSON
client_max_body_size 15M;
